#include <bits/stdc++.h>

using namespace std;

int main()
{
    int T, y, a;
    cin >> T;
    int vet[1000];
    
    for (y = 0, a = 0; y < 1000; y++){
        cout << "N[" << y << "] = " << a << endl;
        
        a++;
        
        if(a==T)
        
           a=0;
        }
}